rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Simple salon app: no Firebase Auth passwords. Tighten in production with App Check / Cloud Functions.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
